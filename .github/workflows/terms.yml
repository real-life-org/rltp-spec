name: terms

# terms/rltp.skos.jsonld is this specification's concept scheme. The mappings to the other
# parts of Real Life live in real-life-org/meta; a change here must still fit them, so the
# register's guard runs against the working copy before it is merged.

on:
  pull_request:
    paths: [terms/rltp.skos.jsonld]
  push:
    branches: [main]
    paths: [terms/rltp.skos.jsonld]

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: real-life-org/meta
          path: meta-src            # the register, beside the working copy
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: fetch the concept schemes meta pins
        # The script arrives with real-life-org/meta#3; until then every world uses its seed.
        run: test -f meta-src/scripts/fetch_worlds.py && python3 meta-src/scripts/fetch_worlds.py || echo "meta pins no world yet"
      - name: concept scheme matches the shared register
        run: python3 meta-src/scripts/guard.py --scheme rltp=terms/rltp.skos.jsonld
