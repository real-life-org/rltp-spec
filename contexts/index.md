# RLTP JSON-LD Context

The pinned RLTP JSON-LD context. Implementations MUST use the pinned document, never a live fetch.

- [`rltp-v1.jsonld`](rltp-v1.jsonld)

Back to the [specification index](../).
