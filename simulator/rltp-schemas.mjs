// rltp-schemas.mjs — SHIM: das Schema-Bundle kommt seit Faden 3
// (29.08.2026) aus der Bibliothek (lib/src/schemas.ts, generiert aus
// schemas/*.json → ./lib/schemas.js, beide frischegeprüft). schemas/
// bleibt die einzige Wahrheitsquelle.
export { SCHEMAS } from './lib/schemas.js'
