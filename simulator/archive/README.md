# Archived simulator probes

`graph.mjs` + `graph-scenario.mjs`: the pre-fresh-always node probe
of the graph simulator (self-anchor enactments). Superseded by the
fresh-always engine (`../engine.mjs`) and its living probes
(`../scenario.mjs`, `../scenario-visibility.mjs`,
`../adversarial.mjs`); the browser graph core `../graph-web.mjs`
(+ its test) remains alive. Preserved unchanged, like spec/archive/:
history is never rewritten. Do not run against the current engine —
they predate rootIkm-based pair-context derivation.
